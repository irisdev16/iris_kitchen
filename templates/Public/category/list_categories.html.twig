{% extends "public/base.html.twig" %}


{% block title %}Accueil | Les recettes d'Iris{% endblock %}

{% block body %}

    <main>

        <h1>Toutes les catégories</h1>

        {% for category in categories %}

            <article>
                <p>{{ category.title }}</p>
            </article>
        {% endfor %}
    </main>

{% endblock %}